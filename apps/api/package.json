{
  "name": "@opensearch/api",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP Search API service for OpenSearch",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "start": "node ./dist/index.js",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@opensearch/shared": "*",
    "@opensearch/ranking": "*",
    "@opensearch/storage": "*"
  }
}
